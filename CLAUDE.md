# triage-lens — プロジェクト規範

## このプロジェクトは何か
スキャナ（Trivy / osv-scanner）が出力した脆弱性リストを、公開データ
（NVD / EPSS / CISA KEV）で優先順位付けし、トリアージレポート
（Markdown）を生成するCLIツール。

- リポジトリ: github.com/secleeman/triage-lens
- 言語: Python 3.11+
- ライセンス: MIT
- オーナーはコードを読まない。品質は自動テストと動作確認で担保する。

## 絶対ルール
1. **mainに直接pushしない**。必ずfeature branch → PR。マージはオーナーの明示承認を得て行う（実行はClaude Code可）。
2. **Phase単位で進める**。現在のPhaseの要件（docs/requirements-p1.md等）の範囲外を実装しない。
3. **テストなしのコードを書かない**。pytest必須。CIが緑になるまでPRを完成扱いしない。
4. **外部APIはNVD/EPSS(FIRST.org)/CISA KEVの公開エンドポイントのみ**。認証キー不要の範囲で使う。レート制限を尊重し、リトライとキャッシュを実装する。
   - このルールは **triage-lens本体の実行時データソース** に適用する。
     `scripts/` 配下の運用スクリプトは、**認証不要の公開APIに限り**利用してよい。
   - 例外: **`--ai` 機能の Claude API のみ**。APIキーは利用者が提供し（環境変数）、
     送信するデータは `docs/requirements-p3.md` の表に固定する。
5. **秘密情報・実在組織の事例を書かない**。テストデータは公開CVE＋架空のプロジェクト名のみ。
6. **依存パッケージは最小限**。追加時はPR説明に理由を書く。
7. **ハードコード禁止**。特定CVE・特定日付に依存する分岐を作らない。
8. コミットは小さく、無関係な変更を混ぜない。

## オーナーへの報告形式
PR説明には必ず以下を書く（オーナーはこれだけ読む）:
- 何ができるようになったか（1-3行、非エンジニア向けの言葉で）
- 動作確認コマンド（コピペで打てる形）
- 期待される出力の例
- テスト結果（passed件数）

## 品質ゲート
- pytest全緑
- ruffでlintエラーなし
- GitHub ActionsでCI（テスト+lint）が通ること
